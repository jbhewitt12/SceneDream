# Leave VITE_API_URL blank for local `npm run dev` so requests go through the
# Vite proxy and avoid localhost vs 127.0.0.1 CORS mismatches.
VITE_API_URL=
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
MAILCATCHER_HOST=http://localhost:1080
